# React + TypeScript + Vite

## Getting start
- Install PNPM
```shell
npm i -g pnpm@latest
```
- Install dependencies
```shell
pnpm i
```
- Start in standard mode
```shell
pnpm start
```
- Start in dev mode
```shell
pnpm dev
```
